package advance;

/**
 * @author pajaiswa - LRU cache supports get(key) and set(key, value) methods.s When the cache
 *         reaches its limit the least used element is removed from it
 */
public class LRUCache {

}
